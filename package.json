{
  "name": "uk-mortgage-rates",
  "version": "1.0.0",
  "private": true,
  "description": "Bank of England benchmark mortgage rates — Cloudflare worker backend for the Remortgage Countdown app",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node test/run.mjs",
    "typecheck": "tsc --noEmit",
    "ingest": "node scripts/ingest.mjs",
    "check-live": "node scripts/check-live.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
